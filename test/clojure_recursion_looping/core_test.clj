(ns clojure-recursion-looping.core-test
  (:require [clojure.test :refer :all]
            [clojure-recursion-looping.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
